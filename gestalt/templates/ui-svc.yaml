apiVersion: v1
kind: Service
metadata:
  name: gestalt-ui
  labels:
    gestalt-app: ui
    gestalt-deployment: {{ .Values.deploymentLabel }}
spec:
  type: {{ .Values.PublicServiceType }}
  selector:
    gestalt-app: ui
  ports:
    - name: http
      port: 80
